import 'package:flutter/material.dart';

class ColocationPage extends StatelessWidget {
  const ColocationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
